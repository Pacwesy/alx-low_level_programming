#include "main.h"
/**
 *print_sign - prints sign based on conditional argument
 *
 *@n: checks for input number
 *return: int.
 */
int print_sign(int n)
{
if (n > 0)
{
_putchar(45);
return (-1);
}
else
{
_putchar(48);
return (0);
}
}
